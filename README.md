# Bots AI Server — 便携版使用指南

欢迎使用 **Bots AI Server**！这是一个**免安装、绿色便携**的 AI 对话服务，下载后解压即可运行，无需安装任何运行时环境（Node.js、Python 等）。

---

## 📦 一、下载与解压

### 1. 获取压缩包
从发布渠道下载 **`BotsAI-Portable.zip`** 压缩包。

### 2. 解压到任意位置
将压缩包解压到 Windows 电脑的任意目录，例如：

```
D:\BotsAI
C:\Users\你的用户名\Desktop\BotsAI
```

> 💡 **建议**：解压到不含中文和空格的路径（如 `D:\BotsAI`），可避免潜在的路径问题。

解压后你会看到以下文件结构：

```
BotsAI/
├── .bun/              # Bun 运行时（内置，无需安装）
├── src/               # 后端源码
├── public/            # 前端页面（聊天界面、状态监控页）
├── node_modules/      # 依赖库（内置）
├── .env               # 配置文件（可修改）
├── start.bat          # 启动脚本（双击运行）
├── package.json
├── bun.lock
├── tsconfig.json
└── README.md          # 本说明文档
```

---

## ⚙️ 二、配置参数（运行前必读）

> ⚠️ **首次使用，请先完成本章配置，再启动服务**。否则服务可能无法正常调用 AI 接口。

所有配置都保存在 **`.env`** 文件中。使用**记事本**（或任意文本编辑器）打开解压目录下的 `.env` 文件进行修改。

### 2.1 完整配置项说明

```ini
# ============================================
# 应用配置
# ============================================
APP_PORT=3001

# ============================================
# LLM 配置
# ============================================
LLM_API_BASE=https://api.deepseek.com
LLM_API_KEY=sk-apikey
LLM_MODEL=deepseek-v4-flash
# 思考模式（true=开启，false/留空=关闭）
LLM_THINKING=true
LLM_REASONING_EFFORT=high
```

| 配置项 | 说明 | 示例 | 是否必填 |
|--------|------|------|---------|
| `APP_PORT` | 服务监听端口 | `3001` | ✅ 必填 |
| `LLM_API_BASE` | LLM 服务 API 地址 | `https://api.deepseek.com` | ✅ 必填 |
| `LLM_API_KEY` | 你的 API 密钥（**必须修改**） | `sk-xxxxxxxx` | ✅ 必填 |
| `LLM_MODEL` | 使用的模型名称 | `deepseek-v4-flash` | ✅ 必填 |
| `LLM_THINKING` | 思考模式开关 | `true` / `false` | 可选 |
| `LLM_REASONING_EFFORT` | 推理强度 | `high` / `medium` / `low` | 可选 |

### 2.2 修改 API Key（必做）

1. 用记事本打开 `.env`
2. 找到 `LLM_API_KEY=sk-apikey` 这一行
3. 将 `sk-apikey` 替换为**你自己的真实 API 密钥**，例如：
   ```
   LLM_API_KEY=sk-1234567890abcdef
   ```
4. **保存文件**（注意：保存为 UTF-8 编码，不要另存为带 BOM 的格式）

> ⚠️ **重要**：`sk-apikey` 是占位符，**不修改将无法正常调用 AI 服务**。

### 2.3 修改端口（可选）

默认端口为 `3001`。如需修改，将 `APP_PORT` 改为其他值，例如：

```
APP_PORT=8080
```

修改后，访问地址变为 `http://localhost:8080/`。

> 💡 如果 `3001` 端口被其他程序占用，服务会启动失败，此时修改端口即可解决。

### 2.4 修改模型（可选）

根据你的 LLM 服务商支持的模型，修改 `LLM_MODEL` 和 `LLM_API_BASE`：

```
LLM_API_BASE=https://api.deepseek.com
LLM_MODEL=deepseek-chat
```

> 💡 本项目兼容 **OpenAI 格式**的 API 接口，只要服务商提供兼容接口即可接入。

### 2.5 配置完成后

配置保存后，请继续阅读下一章 **启动服务**，开始使用。

---

## 🚀 三、启动服务

### 1. 双击运行
找到 **`start.bat`** 文件，**双击**它即可启动服务。

启动后，命令行窗口会显示以下信息：

```
============================================================
🚀 Bots 服务启动成功！
============================================================
   地址:      http://localhost:3001
   健康检查:  http://localhost:3001/api/health
   对话接口:  http://localhost:3001/api/chat/stream
   聊天页面:  http://localhost:3001/
   状态监控:  http://localhost:3001/status.html
============================================================
```

### 2. 访问服务
服务启动后，打开浏览器访问：

| 功能 | 地址 |
|------|------|
| **聊天页面** | http://localhost:3001/ |
| **状态监控** | http://localhost:3001/status.html |
| **健康检查** | http://localhost:3001/api/health |

> ⚠️ **注意**：请保持 `start.bat` 的命令行窗口**处于开启状态**。关闭窗口即停止服务（不要最小化后忽略它）。

---

## ⏹ 四、停止服务

停止服务有以下两种方式：

### 方式一：快捷键（推荐）
在 `start.bat` 的命令行窗口中，按下 **`Ctrl + C`**，然后按提示输入 `Y` 确认退出。

### 方式二：直接关闭窗口
直接点击命令行窗口右上角的 **`×`** 关闭按钮。

> 💡 服务支持**优雅关闭**，会安全释放资源后退出，两种方式均可放心使用。

---

## ❓ 五、常见问题（FAQ）

### Q1：双击 `start.bat` 后窗口一闪而过？
- 可能是 `.bun\bun.exe` 缺失或 `.env` 配置不完整。请确认解压完整，且 `.env` 中必填项已填写。

### Q2：启动时报错"以下环境变量未设置"？
- 说明 `.env` 中必填项（`APP_PORT`、`LLM_API_BASE`、`LLM_API_KEY`、`LLM_MODEL`）缺失或为空，请检查并填写后重启。

### Q3：页面能打开，但对话无响应？
- 请检查 `LLM_API_KEY` 是否正确，以及 `LLM_API_BASE` 和 `LLM_MODEL` 是否匹配你的服务商。

### Q4：端口被占用怎么办？
- 修改 `.env` 中的 `APP_PORT` 为其他未占用端口（如 `8080`、`9000`），保存后重启。

### Q5：如何查看服务运行状态？
- 访问 `http://localhost:3001/status.html` 查看状态监控页，或访问 `http://localhost:3001/api/health` 查看健康检查接口返回的 JSON。

---

## 🗑 六、卸载

本软件为**绿色便携版**，卸载只需**删除解压出来的整个文件夹**即可，不会在系统留下任何残留（无注册表项、无系统服务）。

---

## 📄 七、技术信息（供参考）

- **运行时**：内置 Bun（免安装）
- **Web 框架**：Hono
- **配置方式**：`.env` 文件
- **兼容接口**：OpenAI 兼容 API

---

*如有其他问题，请联系项目维护者。*
